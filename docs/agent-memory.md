# Agent Memory

Durable legacy-latex facts for future agent sessions.

## Facts

- Last verified: 2026-07-02 from `README.md` and `AGENTS.md`.
- This is a research-notes/transcription workspace with subject folders that separate `src/`, `refs/`, and `build/` assets.
- Prefer `./bin/build.sh <project>` or Makefile shortcuts for standard builds; direct `latexmk` commands are also documented in `AGENTS.md`.
- Keep reference assets in `refs/`; run output/reference text comparisons in `/tmp` instead of committing comparison artifacts.
- Preserve generated PDFs unless the user explicitly asks to remove them.
